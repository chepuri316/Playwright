// syntax of ternary operator 
// Ternary = condition ? "true resul" : "false result";

let a =10;b=30;

let valuecomparison  = a>b ? "a is grter than b" : "a is not grter than b" ;
console.log(valuecomparison);